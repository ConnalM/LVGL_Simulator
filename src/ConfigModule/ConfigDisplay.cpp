#include "ConfigDisplay.h"
// #include <YourDisplayLibrary.h>

void ConfigDisplay::showSettings(const ConfigSettings& settings) {
    // Replace with your display code
    // Example: display.print("Laps: "); display.println(settings.numLaps);
}

void ConfigDisplay::showMessage(const char* msg) {
    // Replace with your display code
    // Example: display.println(msg);
}
